% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen_zip.R
\name{screen_zip}
\alias{screen_zip}
\title{Screen a ZIP archive for upload to EES}
\usage{
screen_zip(
  zippath,
  log_key = NULL,
  log_dir = "./",
  dd_checks = TRUE,
  verbose = FALSE,
  stop_on_error = FALSE
)
}
\arguments{
\item{zippath}{A character string giving the path to the ZIP file.}

\item{log_key}{Keystring for creating log file. If given, the screening will
write a log file to disk called eesyscreening_log_<log_key>.json default=NULL}

\item{log_dir}{Directory within which to place the log file. default="./"}

\item{dd_checks}{Run the Data dictionary tests, default=TRUE (this is
implemented to allow devs
to update robot test data to be consistent with data dictionary tests).}

\item{verbose}{logical, if TRUE prints feedback messages to console for
every test, if FALSE run silently}

\item{stop_on_error}{logical, if TRUE will stop with an error if the result
is "FAIL", and will throw genuine warning if result is "WARNING"}
}
\value{
A list containing:
\itemize{
\item \code{structure_results} — data frame from \code{screen_zip_structure()}
\item \code{pair_results} — named list of \code{screen_csv()} results, one per
pair, named by the data file stem
\item \code{overall_stage} — \code{"ZIP structure"} if structure checks failed,
\code{"Passed"} if all pairs passed, otherwise the failing stage from the
first failing pair
\item \code{passed} — \code{TRUE} only if all structure checks and all pairs passed
\item \code{api_suitable} — \code{TRUE} only if all structure checks and all pairs
are API suitable
}

If \code{screen_zip_structure()} finds any FAIL the function returns early with
\code{passed = FALSE} and \code{overall_stage = "ZIP structure"} before extracting
the archive.
}
\description{
Validates the ZIP structure with \code{screen_zip_structure()}, then extracts
the archive and runs \code{screen_csv()} on every data/meta pair inside it.
All pairs are always screened — there is no early exit once the structure
checks pass.
}
\examples{
\dontrun{
# Build a single-pair ZIP from the built-in example data
tmp_zip <- tempfile(fileext = ".zip")
d <- tempfile()
dir.create(d)
write.csv(example_data, file.path(d, "ex.csv"), row.names = FALSE)
write.csv(example_meta, file.path(d, "ex.meta.csv"), row.names = FALSE)
zip::zip(tmp_zip, c("ex.csv", "ex.meta.csv"), root = d)

res <- screen_zip(tmp_zip, verbose = TRUE)
res$passed
}
}
