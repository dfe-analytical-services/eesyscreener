% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen_zip.R
\name{screen_zip}
\alias{screen_zip}
\title{Screen a ZIP archive for upload to EES}
\usage{
screen_zip(
  zippath,
  log_key = NULL,
  log_dir = "./",
  dd_checks = TRUE,
  verbose = FALSE,
  stop_on_error = FALSE
)
}
\arguments{
\item{zippath}{A character string giving the path to the ZIP file.}

\item{log_key}{Keystring for creating log file. If given, the screening will
write a log file to disk called eesyscreening_log_<log_key>.json default=NULL}

\item{log_dir}{Directory within which to place the log file. default="./"}

\item{dd_checks}{Run the Data dictionary tests, default=TRUE (this is
implemented to allow devs
to update robot test data to be consistent with data dictionary tests).}

\item{verbose}{logical, if TRUE prints feedback messages to console for
every test, if FALSE run silently}

\item{stop_on_error}{logical, if TRUE will stop with an error if the result
is "FAIL", and will throw genuine warning if result is "WARNING"}
}
\value{
A named list. The first element, \code{zip_structure}, is a data frame
of ZIP structure check results (columns \code{check}, \code{result}, \code{message},
\code{guidance_url}, \code{stage}), one row per check. Each subsequent element is a
\code{screen_csv()} result named by the data file stem (e.g. \code{result$my_data}).
If any structure check fails the function returns early with only
\code{zip_structure} present and no pair entries.
}
\description{
Validates the ZIP structure by running the \verb{check_zip_*} family of checks,
then extracts the archive and runs \code{screen_csv()} on every data / meta pair
inside it. All pairs are always screened — there is no early exit once the
structure checks pass.
}
\examples{
\dontrun{
tmp_zip <- tempfile(fileext = ".zip")
d <- tempfile()
dir.create(d)
write.csv(example_data, file.path(d, "ex.csv"), row.names = FALSE)
write.csv(example_meta, file.path(d, "ex.meta.csv"), row.names = FALSE)
zip::zip(tmp_zip, c("ex.csv", "ex.meta.csv"), root = d)

res <- screen_zip(tmp_zip, verbose = TRUE)
res$ex$passed
}
}
