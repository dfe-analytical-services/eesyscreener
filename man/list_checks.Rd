% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_checks.R
\name{list_checks}
\alias{list_checks}
\title{List all checks run by screen_csv()}
\usage{
list_checks(print = TRUE)
}
\arguments{
\item{print}{logical. If \code{TRUE} (default), a formatted table is printed to
the console and the data frame is returned invisibly. If \code{FALSE}, the data
frame is returned directly without printing.}
}
\value{
A data frame with columns:
\describe{
\item{function_name}{Name of the check or precheck function}
\item{title}{Title from the function's documentation}
\item{stage}{Stage the function belongs to, as used in \code{\link[=screen_dfs]{screen_dfs()}}}
}
Returned invisibly when \code{print = TRUE}.
}
\description{
Returns the full list of \code{check_} and \code{precheck_} functions in the order
they are executed by \code{\link[=screen_csv]{screen_csv()}}, along with the title from each
function's documentation and the stage they belong to. The list is derived
dynamically from the bodies of \code{\link[=screen_filenames]{screen_filenames()}} and \code{\link[=screen_dfs]{screen_dfs()}}, so
it always reflects the current execution order.
}
\examples{
list_checks()
list_checks(print = FALSE)
}
