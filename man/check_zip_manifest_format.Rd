% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_zip_manifest_format.R
\name{check_zip_manifest_format}
\alias{check_zip_manifest_format}
\title{Check that dataset_names.csv has the correct format}
\usage{
check_zip_manifest_format(manifest_df, verbose = FALSE, stop_on_error = FALSE)
}
\arguments{
\item{manifest_df}{A data frame read from \code{dataset_names.csv}.}

\item{verbose}{logical, if TRUE prints feedback messages to console for
every test, if FALSE run silently}

\item{stop_on_error}{logical, if TRUE will stop with an error if the result
is "FAIL", and will throw genuine warning if result is "WARNING"}
}
\value{
a single row data frame
}
\description{
Validates that the manifest data frame has exactly the columns
\code{file_name} and \code{dataset_name} (in that order), is non-empty, contains no
NA values, and has no duplicate \code{file_name} entries.
}
\examples{
good <- data.frame(file_name = "data", dataset_name = "My data")
check_zip_manifest_format(good)

bad <- data.frame(file = "data", name = "My data")
check_zip_manifest_format(bad)
}
\seealso{
Other check_zip: 
\code{\link{check_zip_flat_structure}()},
\code{\link{check_zip_manifest_presence}()},
\code{\link{check_zip_no_unreferenced}()},
\code{\link{check_zip_pairs}()},
\code{\link{check_zip_readable}()}
}
\concept{check_zip}
