% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_geog_combos.R
\name{check_geog_la_combos}
\alias{check_geog_la_combos}
\title{Check local authority code and name combinations}
\usage{
check_geog_la_combos(data, verbose = FALSE, stop_on_error = FALSE)
}
\arguments{
\item{data}{A data frame of the data file}

\item{verbose}{logical, if TRUE prints feedback messages to console for
every test, if FALSE run silently}

\item{stop_on_error}{logical, if TRUE will stop with an error if the result
is "FAIL", and will throw genuine warning if result is "WARNING"}
}
\value{
a single row data frame
}
\description{
Checks that all old_la_code, new_la_code, and la_name combinations in the
data file are valid against the standard geographies lookup.
}
\details{
No geographic_level distinction is made (unlike most other geography checks).
All rows are filtered the same way before checking: rows where old_la_code
is NA are excluded, and rows where new_la_code is "x" (the GSS
not-available code) are excluded. All remaining combinations — including
those with blank or NA new_la_code — are checked and will fail if not found
in the lookup.

Rows where old_la_code is "z" combined with a valid
\code{acceptable_extra_geog_options} entry are also accepted, to accommodate
geographies that have no old LA code.

The \code{restricted_level} parameter is not used here because the
old_la_code NA filter only applies when restricted_level is NULL. If
"Local authority" were set as the restricted level, LA-level rows with a
NA old_la_code would be checked without that filter and incorrectly fail,
since not all LAs have an old code.

If any of the three LA columns is absent from the data, the check passes
immediately.
}
\examples{
check_geog_la_combos(example_data)
check_geog_la_combos(example_data, verbose = TRUE)
}
\seealso{
Other check_geog: 
\code{\link{check_geog_country_combos}()},
\code{\link{check_geog_eda_combos}()},
\code{\link{check_geog_ignored_rows}()},
\code{\link{check_geog_lad_combos}()},
\code{\link{check_geog_lep_combos}()},
\code{\link{check_geog_lsip_combos}()},
\code{\link{check_geog_na}()},
\code{\link{check_geog_na_code}()},
\code{\link{check_geog_other_code_dupes}()},
\code{\link{check_geog_pcon_combos}()},
\code{\link{check_geog_region_combos}()},
\code{\link{check_geog_region_for_la}()},
\code{\link{check_geog_region_for_lad}()},
\code{\link{check_geog_ward_combos}()}
}
\concept{check_geog}
