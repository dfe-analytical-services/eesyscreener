% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen_zip_structure.R
\name{screen_zip_structure}
\alias{screen_zip_structure}
\title{Run all ZIP structure checks against a ZIP file}
\usage{
screen_zip_structure(zippath, verbose = FALSE, stop_on_error = FALSE)
}
\arguments{
\item{zippath}{A character string giving the path to the ZIP file.}

\item{verbose}{logical, if TRUE prints feedback messages to console for
every test, if FALSE run silently}

\item{stop_on_error}{logical, if TRUE will stop with an error if any check
returns "FAIL", and will throw genuine warning if any returns "WARNING"}
}
\value{
A data frame with columns \code{check}, \code{result}, \code{message},
\code{guidance_url}, and \code{stage}, one row per check run. If
\code{check_zip_readable()} fails, only that single row is returned.
}
\description{
Orchestrates the \verb{check_zip_*} family of checks. Runs \code{check_zip_readable()}
first and returns early if it fails (subsequent checks require a readable
ZIP). Otherwise, runs all remaining structure checks and returns a combined
results data frame.
}
\details{
The \code{stage} column is set to \code{"ZIP structure"} for every row, mirroring
how \code{screen_filenames()} and \code{screen_dfs()} attach their stage labels.
}
\examples{
\dontrun{
screen_zip_structure("path/to/upload.zip")
}
}
