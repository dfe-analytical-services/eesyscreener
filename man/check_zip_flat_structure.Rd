% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_zip_flat_structure.R
\name{check_zip_flat_structure}
\alias{check_zip_flat_structure}
\title{Check that a ZIP file has a flat (non-nested) structure}
\usage{
check_zip_flat_structure(file_entries, verbose = FALSE, stop_on_error = FALSE)
}
\arguments{
\item{file_entries}{A character vector of file paths as returned by
\code{zip::zip_list(zippath)$filename}.}

\item{verbose}{logical, if TRUE prints feedback messages to console for
every test, if FALSE run silently}

\item{stop_on_error}{logical, if TRUE will stop with an error if the result
is "FAIL", and will throw genuine warning if result is "WARNING"}
}
\value{
a single row data frame
}
\description{
Verifies that no file entry within the ZIP is inside a subdirectory.
Directory entries (trailing \code{/}) are normalised before checking. Any entry
that still contains a \code{/} after normalisation is considered nested and
causes a FAIL. This catches nested folders, OS artefacts such as
\verb{__MACOSX/._file}, and any other non-root paths.
}
\examples{
check_zip_flat_structure(c("data.csv", "data.meta.csv"))
check_zip_flat_structure(c("data.csv", "subdir/data.meta.csv"))
}
\seealso{
Other check_zip: 
\code{\link{check_zip_manifest_format}()},
\code{\link{check_zip_manifest_presence}()},
\code{\link{check_zip_no_unreferenced}()},
\code{\link{check_zip_pairs}()},
\code{\link{check_zip_readable}()}
}
\concept{check_zip}
